#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gh-pages/bin/gh-pages" "$@"
  ret=$?
else 
  node  "$basedir/../gh-pages/bin/gh-pages" "$@"
  ret=$?
fi
exit $ret
